{
  "name": "data-mapper",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run babel && npm run babel-tests && node compiled-tests | ./node_modules/tap-spec/bin/cmd.js",
    "babel-tests": "rm -rf compiled-tests && babel tests -d compiled-tests",
    "babel": "rm -rf lib && babel src -d lib"
  },
  "author": "Rhys van der Waerden",
  "license": "to be announced",
  "devDependencies": {
    "babel": "^5.8.23",
    "tap-spec": "^4.1.0",
    "tape": "^4.2.0",
    "tape-catch": "^1.0.4"
  },
  "dependencies": {
    "lodash": "^3.10.1"
  }
}
